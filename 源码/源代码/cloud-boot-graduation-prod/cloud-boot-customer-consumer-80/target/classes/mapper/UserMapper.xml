<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fatcats.top.dao.UserAccountDao">

    <!--

        //查询所有用户
    public List<UserAccount> findAllUser();
    //根据用户名查询用户
    public UserAccount findUserByName(@Param("user_name") String user_name);
    //新增用户
    public void addUser(UserAccount userAccount);
    //删除用户
    public void delUserByID(@Param("user_id") Integer id);
    //修改用户
    public void uptUserByID(@Param("user_id") Integer id);
    //根据用户ID查询用户
    public UserAccount findUserByID(@Param("user_id") Integer id);
    -->

<select id="findAllUser" resultType="UserAccount">

   SELECT
    user_id , user_name , user_pwd,user_phone , user_power
    FROM
    user_account

</select>

<delete id="delUserByID">

DELETE FROM
 user_account
  WHERE
  user_id = #{user_id}

</delete>

<select id="findUserByName" resultType="UserAccount">

select
 user_id , user_name , user_pwd,user_phone , user_power

from
      user_account
where
     user_name = #{user_name}

</select>

<insert id="addUser" parameterType="UserAccount" useGeneratedKeys="true">

INSERT INTO
user_account
VALUES
(DEFAULT ,#{user_name},#{user_pwd},#{user_phone},#{user_power})

</insert>


<update id="uptUserByID">
UPDATE
 user_account
  SET
user_name = #{user_name},
user_pwd = #{user_pwd},
user_phone = #{user_phone},
user_power = #{user_power}
WHERE
user_id = #{user_id}
</update>


<select id="findUserByID" resultType="UserAccount">

SELECT user_id , user_name , user_pwd , user_phone , user_power
FROM
user_account
WHERE
user_id = #{user_id}

</select>
</mapper>

